

Date: Dec, 13, 2017
Language: scala

The ’top40.sql’ is the data.
The ‘./src/main/scala’ contains the main code ’TextAnalysis.scala’
‘build.sbt’ shows the name, organization, version and scalaVersion(2.12.3).

Run code:
You can change the path to the root of the code. Then input ’sbt’ in terminal. Then input ‘run’ and you can get the result. I select K=5 and you can change it.

I have test the code in my own laptop which operate system is ‘OS X Yosemite(10.10.5)’